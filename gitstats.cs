using System;
using Gitstats;
namespace Gitstats
{
    public class GitstatClient
    {
            public static void GetUserJSON(String Username)
            {
            user.call(Username);
            }
    }
}
